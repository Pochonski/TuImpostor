/** @type {import('prettier').Config} */
const prettierConfig = {
  semi: true,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: "all",
  printWidth: 100,
  arrowParens: "avoid",
  endOfLine: "lf",
};

export default prettierConfig;
